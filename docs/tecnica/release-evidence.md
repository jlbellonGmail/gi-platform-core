# Release evidence

La evidencia agregada de una release vive en
`runs/<version>/release-readiness/` y se genera sólo después de que el HEAD de
`develop` contiene todas las unidades incluidas. El manifiesto enumera los
items cerrados que forman el alcance; el gate continúa siendo read-only.
