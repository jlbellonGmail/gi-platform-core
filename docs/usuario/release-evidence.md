# Evidencia de release

Antes de publicar, completar `SUMMARY.md`, `audit-1.md`, `test-report-1.md` y
`code-review-1.md` en `runs/<version>/release-readiness/`, con veredictos
`approved` y referencias al HEAD candidato. Ejecutar luego
`scripts/release-readiness.ps1 -Version <version> -DryRun`.
