# Decisión

Se corrige el disparo del cierre, no la protección de `develop`: el dispatch
manual sólo prepara los datos y el script `close-feature.ps1` mantiene las
validaciones de PR mergeada, base, rama, estado de ROADMAP y publicación.

