# Decisión

Se adopta la derivación dinámica de la ruta desde los outputs `version` y
`slug` del paso que analiza la rama. No se agrega una constante de versión ni
se cambia el contrato de autorización.

